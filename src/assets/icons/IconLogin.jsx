import iconLogin from '../icon/login.png'

const IconLogin = ({ stylesLogin }) => {
    
    return (
        <div className={stylesLogin}>
            <img  src={iconLogin} alt="login" />
        </div>
    )
};

export default IconLogin;